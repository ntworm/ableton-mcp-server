"""Offline probe group (Phase 2 stub).

Phase 2 subagent extracts offline probe logic from
``ableton_mcp_server/acceptance/runner.py`` into this module.

This file is intentionally minimal in Phase 1: it exists so the
package import graph resolves. The full implementation will register
TOOLS, run_offline_probes, and any helpers it needs.
"""

from __future__ import annotations

__all__: list[str] = []