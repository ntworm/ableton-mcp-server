"""Mutations probe group (Phase 2 stub).

Phase 2 subagent extracts 29 mutation probes + their restore
registrations from ``runner.py`` into this module. This is the
largest and most complex probe group; the subagent owns the
``register_probes`` and ``register_restores`` API per the design spec.
"""

from __future__ import annotations

__all__: list[str] = []
