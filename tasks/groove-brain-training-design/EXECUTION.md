# Execution packet: groove-brain-training-design

## Target

- Project: `ableton-mcp-server`
- Authorized root: `C:\Users\Usuario\repos\ableton-mcp-server`
- Outcome: consolidate one canonical, evidence-backed design for the Groove Brain dataset and neural-training program without changing product code or starting training.

## Repository context

- Local `main` contains the committed Groove Brain Gate 0 work after public `v0.6.0` and unrelated tracked/untracked prototype changes.
- Persistent repository context is stale; current source, focused Git history, existing Groove Intelligence evidence, and primary external sources are authoritative for this task.
- Existing dirty source, seed, Remote Script, server, journey, scratch, and ingestion paths are preserved and excluded.

## Scope

- Included: corpus facts; user-declared authorship; canonical training representation; exact/canonical/near dedupe; family-aware splits; model bake-off; training ladder; evaluation; anti-copy; reproducibility; ONNX/runtime boundary; hardware and disk gates; implementation-program decomposition.
- Excluded: product code, training execution, model downloads, dependency installation, corpus mutation, seed replacement, cleanup, push, release, and publication.
- User-approved direction: drums/rhythm only; offline local product; `.ablx` integration; user-authored corpus; masked conditional Transformer as challenger rather than automatic winner; no coding before the research/design is reviewed.

## Acceptance criteria

- [x] One focused specification supersedes conflicting data/model assumptions for Groove Brain.
- [x] Current implementation, research baseline, and missing training capabilities are explicitly separated.
- [x] Three model approaches are compared under one frozen dataset and evaluation contract.
- [x] Dataset leakage, algorithmic-family repetition, weak labels, humanization limits, and memorization have binary gates.
- [x] Training can stop safely after environment, dataset, tiny-run, bake-off, full-run, or runtime gates.
- [x] No unrelated path or external state changes.

## Plan and routing

- Route: solo documentation work; the user previously requested no subagent for this continuation.
- Durable state: this task bundle and the focused design specification under `docs/superpowers/specs/`.
- Next gate: user reviews the written specification before an implementation plan is authored.

## Verification

- Inspect exact owned diff and repository status.
- Scan owned Markdown for incomplete markers, contradictions, stale rights claims, broken local links, and whitespace errors.
- Verify model/runtime claims against primary papers, project repositories, and official PyTorch/ONNX Runtime documentation.

## Evidence and handoff

- Changed paths: this packet, the canonical `2026-08-31` dataset/training specification, and narrow corrections to the two `2026-08-30` research specifications.
- Primary evidence checked: GrooVAE/GMD, GrooveTransformer, Transformer Groove Infilling, PocketVAE, compound-word conditional drums, PyTorch Blackwell support, and ONNX Runtime execution/optimization/quantization documentation.
- Verification: local Markdown links pass; incomplete-marker scan passes; stale Toontrack-rights claims are absent; owned `git diff --check` passes; unrelated dirty paths remain unstaged and untouched.
- No plan execution begins from this packet without the written-spec user review gate.
